import StubPage from '@/components/layout/stub-page';

export default function VendorsPage() {
  return (
    <StubPage
      title="Vendors"
      breadcrumb={['Inventory', 'Vendors']}
      description="Manage supplier directory, contact details, and purchase history."
    />
  );
}
